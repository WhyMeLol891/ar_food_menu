const loadLayout=window.loadLayout;
const statusText=document.getElementById('status'),instruction=document.getElementById('instruction'),selected=document.getElementById('selected'),startButton=document.getElementById('start-ar');
let layout,renderer,scene,camera,mindar,anchor,activeModel;
function setStatus(message){statusText.textContent=message;}
function showFood(dish){if(activeModel&&anchor)anchor.group.remove(activeModel);const factory=window.FOOD_MODELS[dish.type];if(!factory)return;activeModel=factory();activeModel.scale.setScalar(dish.scale||.25);activeModel.position.set(0,0,.05);anchor.group.add(activeModel);selected.textContent=`${dish.name} found`;instruction.textContent='Keep the menu steady while the dish preview is displayed.';}
async function start(){startButton.disabled=true;setStatus('Loading image target');try{const {MindARThree}=await import('https://esm.sh/mind-ar@1.2.5?bundle');layout=await loadLayout();const dish=layout.dishes.find(item=>item.type!=='ignore');if(!dish)throw new Error('NO_DISHES');mindar=new MindARThree({container:document.getElementById('ar-container'),imageTargetSrc:'assets/targets/menu.mind',maxTrackables:1});({renderer,scene,camera}=mindar);scene.add(new THREE.HemisphereLight(0xffffff,0x496a55,2.5));const key=new THREE.DirectionalLight(0xffffff,3);key.position.set(2,4,3);scene.add(key);anchor=mindar.addAnchor(0);anchor.onTargetFound=()=>{setStatus('Menu found');showFood(dish);};anchor.onTargetLost=()=>{setStatus('Searching for menu');if(activeModel){anchor.group.remove(activeModel);activeModel=null;}};await mindar.start();setStatus('Searching for menu');instruction.textContent='Point the camera at the menu image used to compile the target.';startButton.textContent='Camera active';renderer.setAnimationLoop(()=>{if(activeModel)activeModel.rotation.y+=.008;renderer.render(scene,camera);});}catch(error){setStatus('AR setup failed');const messages={NO_DISHES:'Save at least one dish marker before starting AR.'};instruction.textContent=messages[error.message]||'AR could not start: '+(error.message||'check the target file and camera permission.');startButton.disabled=false;console.error('AR setup failed:',error);}}
startButton.addEventListener('click',start);window.registerFoodModels(THREE);
